management:
  endpoint:
    health:
      probes:
        enabled: true
  server:
    port: 9080
  endpoints:
    web:
      exposure:
        include: '*'
spring:
  application:
    name: ok

server:
  port: 8080
  forward-headers-strategy: framework
  shutdown: graceful

logging:
  level:
    ROOT: warn
    nl.appsource: info
#    org.springframework.web: debug
#    org.springframework.security: debug

